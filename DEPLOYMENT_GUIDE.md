# دليل نشر التحديثات - Educore System

## نظرة عامة

هذا الدليل يشرح كيفية نشر تحديثات البرنامج تلقائياً للمستخدمين عبر GitHub Releases.

---

## 🔑 الإعداد الأولي (مرة واحدة فقط)

### 1. توليد مفاتيح التوقيع

قبل أول إصدار، يجب توليد مفاتيح التوقيع لحماية التحديثات:

```powershell
cd "DAS Frontend"
npm run tauri signer generate -- -w ~/.tauri/myapp.key
```

سيتم إنشاء ملفين:

- **Private Key** (مفتاح خاص): يجب حفظه بأمان ولا يُشارك أبداً
- **Public Key** (مفتاح عام): يوضع في `tauri.conf.json`

### 2. إضافة المفاتيح إلى GitHub Secrets

اذهب إلى: `GitHub Repository → Settings → Secrets and variables → Actions`

أضف السرّين التاليين:

- **TAURI_PRIVATE_KEY**: المحتوى الكامل للمفتاح الخاص
- **TAURI_KEY_PASSWORD**: كلمة المرور (إذا تم إنشاؤها)

### 3. تحديث tauri.conf.json

افتح `DAS Frontend/src-tauri/tauri.conf.json` وضع المفتاح العام:

```json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://github.com/Qais9alnoS/Dasystem2/releases/latest/download/latest.json"
      ],
      "dialog": true,
      "pubkey": "YOUR_PUBLIC_KEY_HERE"
    }
  }
}
```

---

## 📦 نشر تحديث جديد

### الطريقة 1: عبر Git Tags (موصى بها)

```powershell
# 1. تحديث رقم الإصدار في الملفات
# - DAS Frontend/src-tauri/tauri.conf.json → "version": "1.0.1"
# - DAS Frontend/src-tauri/Cargo.toml → version = "1.0.1"
# - DAS Backend/backend/app/main.py → "version": "1.0.1"
# - version.json → "version": "1.0.1"

# 2. حفظ التغييرات
git add .
git commit -m "Release v1.0.1"

# 3. إنشاء Tag
git tag v1.0.1

# 4. رفع التغييرات والـ Tag
git push origin main
git push origin v1.0.1
```

### الطريقة 2: عبر GitHub Actions يدوياً

1. اذهب إلى: `GitHub → Actions → Release Build and Deploy`
2. اضغط `Run workflow`
3. أدخل رقم الإصدار (مثلاً: `1.0.1`)
4. اضغط `Run workflow`

---

## 🔄 كيف يعمل نظام التحديثات؟

### عند المستخدم:

1. عند فتح البرنامج، يتحقق تلقائياً من وجود تحديثات
2. إذا وُجد تحديث جديد، يظهر نافذة تحتوي على:
   - رقم الإصدار الجديد
   - ملاحظات التحديث (Release Notes)
   - زر "تحديث الآن" أو "لاحقاً"
3. عند الضغط على "تحديث الآن":
   - يتم تحميل التحديث مع شريط تقدم
   - يتم التحقق من التوقيع الرقمي
   - يتم تثبيت التحديث تلقائياً
   - يُعاد تشغيل البرنامج

### على GitHub:

1. عند رفع Tag جديد، يبدأ GitHub Actions تلقائياً
2. يتم بناء Backend (Python → EXE)
3. يتم بناء Frontend (Tauri → MSI)
4. يتم إنشاء Release جديد مع الملفات:
   - `Educore_1.0.1_x64_en-US.msi` (ملف التثبيت)
   - `Educore_1.0.1_x64_en-US.msi.zip` (ملف التحديث المضغوط)
   - `Educore_1.0.1_x64_en-US.msi.zip.sig` (التوقيع الرقمي)
   - `latest.json` (معلومات التحديث)

---

## 📝 أمثلة على Release Notes

عند إنشاء Release، يمكنك إضافة ملاحظات بالعربية:

```markdown
## التحديثات الجديدة

### ✨ ميزات جديدة

- إضافة نظام التحديثات التلقائية
- تحسين واجهة لوحة المدير

### 🐛 إصلاحات

- إصلاح مشكلة عرض الطلاب في الجلسة الخاطئة
- تحسين أداء تحميل البيانات

### 🔧 تحسينات

- تحسين سرعة البحث
- تحديث المكتبات الأساسية
```

---

## 🔍 التحقق من التحديثات

### للمطورين - اختبار محلي:

```powershell
# تشغيل البرنامج في وضع التطوير
cd "DAS Frontend"
npm run tauri dev
```

### للمستخدمين - التحقق اليدوي:

يمكن للمستخدم التحقق من الإصدار الحالي من:

- قائمة المساعدة → حول البرنامج
- أو من خلال endpoint: `http://localhost:8000/version`

---

## ⚠️ ملاحظات مهمة

1. **رقم الإصدار**: يجب أن يكون أكبر من الإصدار السابق (1.0.1 > 1.0.0)
2. **المفتاح الخاص**: لا تشاركه أبداً ولا ترفعه على Git
3. **الاختبار**: اختبر التحديث على جهاز تجريبي قبل النشر
4. **النسخ الاحتياطية**: احتفظ بنسخة احتياطية من قاعدة البيانات قبل التحديث

---

## 🆘 حل المشاكل

### المشكلة: التحديث لا يظهر

**الحل**: تأكد من:

- رقم الإصدار في `tauri.conf.json` أكبر من الحالي
- المفتاح العام صحيح في `tauri.conf.json`
- الـ Release تم نشره على GitHub

### المشكلة: فشل التحديث

**الحل**: تأكد من:

- التوقيع الرقمي صحيح
- الملفات موجودة في GitHub Release
- اتصال الإنترنت يعمل

### المشكلة: GitHub Actions فشل

**الحل**: تأكد من:

- Secrets موجودة في GitHub
- الملفات المطلوبة موجودة في المشروع
- لا توجد أخطاء في الكود

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، افتح Issue على GitHub.
