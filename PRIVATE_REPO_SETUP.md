# إعداد التحديثات من ريبو خاص (Private Repository)

## 🔒 المشكلة

الريبو الجديد `https://github.com/Qais9alnoS/educore` هو **ريبو خاص (Private)**، لذلك المستخدمون لن يستطيعوا تحميل التحديثات مباشرة بدون مصادقة.

---

## ✅ الحل: جعل Releases عامة

GitHub يسمح بجعل **Releases عامة** حتى لو كان الريبو خاص. هذه هي الطريقة الموصى بها:

### الخطوات:

#### 1️⃣ إعداد الريبو

في ريبو `educore`:

1. اذهب إلى: **Settings** → **General**
2. ابحث عن قسم **"Danger Zone"**
3. **لا تجعل الريبو عام** - ابقه خاص

#### 2️⃣ جعل Releases عامة تلقائياً

لا يوجد إعداد مباشر لهذا، لكن يمكنك:

**الطريقة الأولى: استخدام GitHub Token (موصى بها)**

1. أنشئ **Personal Access Token** (Classic):
   - اذهب إلى: https://github.com/settings/tokens
   - اضغط **Generate new token** → **Generate new token (classic)**
   - أعطه اسم: `Educore Updates`
   - اختر Scopes:
     - ✅ `repo` (Full control of private repositories)
   - اضغط **Generate token**
   - **احفظ Token** - لن تراه مرة أخرى!

2. أضف Token إلى GitHub Secrets:
   - اذهب إلى: `https://github.com/Qais9alnoS/educore/settings/secrets/actions`
   - أضف Secret جديد:
     - Name: `RELEASE_TOKEN`
     - Value: Token الذي أنشأته

3. تم تحديث Workflow بالفعل لاستخدام `GITHUB_TOKEN` الذي يعمل تلقائياً

**الطريقة الثانية: جعل Releases عامة يدوياً**

بعد كل Release:

1. اذهب إلى: `https://github.com/Qais9alnoS/educore/releases`
2. افتح Release
3. في الإعدادات، تأكد من أن الملفات قابلة للتحميل بدون مصادقة

---

## 🔐 الطريقة البديلة: استخدام GitHub Token في التطبيق

إذا أردت إبقاء Releases خاصة تماماً، يمكنك:

### 1. إنشاء Token للقراءة فقط

```
Scopes:
- ✅ public_repo (للوصول للـ releases العامة فقط)
```

### 2. تضمين Token في التطبيق (غير موصى به)

⚠️ **تحذير**: هذا يعني أن Token سيكون مرئياً في الكود المصدري.

---

## 🎯 الحل الموصى به (الأبسط)

### استخدام GITHUB_TOKEN المدمج

الـ workflow الحالي يستخدم `GITHUB_TOKEN` الذي يُنشأ تلقائياً بواسطة GitHub Actions.

**المشكلة**: `GITHUB_TOKEN` له صلاحيات محدودة على الريبو الحالي فقط.

**الحل**:

1. **جعل Releases عامة** - أسهل طريقة:
   - Releases ستكون قابلة للوصول بدون مصادقة
   - الكود المصدري يبقى خاص
   - المستخدمون يمكنهم تحميل التحديثات مباشرة

2. **كيفية التحقق**:
   - بعد نشر Release، جرب الوصول إلى:
   ```
   https://github.com/Qais9alnoS/educore/releases/latest/download/latest.json
   ```

   - إذا فتح الملف بدون طلب تسجيل دخول = ✅ يعمل
   - إذا طلب تسجيل دخول = ❌ يحتاج إعداد إضافي

---

## 📝 ملاحظات مهمة

### ✅ ما يمكن للمستخدمين الوصول إليه:

- ملفات Release (MSI, JSON, signatures)
- Release notes
- معلومات الإصدار

### ❌ ما لا يمكنهم الوصول إليه:

- الكود المصدري
- Issues
- Pull Requests
- أي شيء آخر في الريبو

---

## 🧪 اختبار النظام

### 1. اختبار محلي:

```powershell
# تحقق من إمكانية الوصول للـ Release
curl https://github.com/Qais9alnoS/educore/releases/latest/download/latest.json
```

إذا حصلت على JSON = ✅ يعمل

### 2. اختبار من التطبيق:

1. ثبّت الإصدار الحالي
2. اذهب إلى الإعدادات
3. اضغط "البحث عن تحديثات"
4. يجب أن يعمل بدون أخطاء

---

## 🔧 إعدادات GitHub الموصى بها

في `https://github.com/Qais9alnoS/educore/settings`:

### General:

- ✅ Repository visibility: **Private**
- ✅ Allow forking: **Disabled** (اختياري)

### Actions:

- ✅ Actions permissions: **Allow all actions**
- ✅ Workflow permissions: **Read and write permissions**

### Releases:

- لا يوجد إعداد خاص - Releases تكون عامة افتراضياً في معظم الحالات

---

## 📋 قائمة التحقق النهائية

قبل نشر أول تحديث:

- [ ] تم نقل الكود إلى ريبو `educore`
- [ ] تم إضافة Secrets (TAURI_PRIVATE_KEY, TAURI_KEY_PASSWORD)
- [ ] تم تحديث URLs في `tauri.conf.json`
- [ ] تم اختبار الوصول إلى Release بدون مصادقة
- [ ] تم اختبار التحديث من التطبيق

---

## 🆘 حل المشاكل

### المشكلة: "Failed to check for updates"

**السبب**: لا يمكن الوصول إلى `latest.json`

**الحل**:

1. تحقق من أن Release تم نشره
2. تحقق من URL في `tauri.conf.json`
3. جرب الوصول يدوياً من المتصفح

### المشكلة: "404 Not Found"

**السبب**: Release غير موجود أو خاص

**الحل**:

1. تأكد من وجود Release في الريبو
2. تأكد من أن الملفات موجودة في Release
3. جرب جعل Release عام يدوياً

---

## 📞 ملخص سريع

**أبسط طريقة للعمل مع ريبو خاص:**

1. ✅ الريبو يبقى خاص
2. ✅ Releases تكون عامة (قابلة للتحميل)
3. ✅ المستخدمون يحصلون على التحديثات بدون مشاكل
4. ✅ الكود المصدري محمي

**لا حاجة لـ:**

- ❌ Tokens في التطبيق
- ❌ مصادقة من المستخدمين
- ❌ إعدادات معقدة

فقط تأكد من أن Releases قابلة للوصول العام!
