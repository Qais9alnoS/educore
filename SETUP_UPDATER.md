# إعداد نظام التحديثات - خطوات التنفيذ

## ✅ ما تم إنجازه

تم إعداد نظام تحديثات تلقائي كامل للبرنامج يتضمن:

1. **Backend Version API** - endpoint للتحقق من إصدار الباك اند
2. **Tauri Updater Plugin** - نظام تحديثات مدمج في Tauri
3. **UpdateChecker Component** - واجهة مستخدم للتحديثات
4. **GitHub Actions Workflow** - بناء ونشر تلقائي
5. **Documentation** - دليل شامل للنشر

---

## 🚀 خطوات التشغيل (يجب تنفيذها بالترتيب)

### الخطوة 1: تثبيت الحزم المطلوبة

```powershell
cd "DAS Frontend"
npm install
```

هذا سيثبت الحزم الجديدة:

- `@tauri-apps/plugin-updater`
- `@tauri-apps/plugin-process`

### الخطوة 2: توليد مفاتيح التوقيع

```powershell
cd "DAS Frontend"
npm run tauri signer generate -- -w updater-keys.key
```

**مهم جداً:**

- سيتم إنشاء ملف `updater-keys.key` (المفتاح الخاص) - **لا ترفعه على Git**
- سيتم طباعة المفتاح العام في الـ console - **احفظه**

### الخطوة 3: تحديث tauri.conf.json

افتح `DAS Frontend/src-tauri/tauri.conf.json` واستبدل `WILL_BE_GENERATED` بالمفتاح العام:

```json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://github.com/Qais9alnoS/Dasystem2/releases/latest/download/latest.json"
      ],
      "dialog": true,
      "pubkey": "ضع_المفتاح_العام_هنا"
    }
  }
}
```

### الخطوة 4: إضافة Secrets إلى GitHub

1. اذهب إلى: `https://github.com/Qais9alnoS/Dasystem2/settings/secrets/actions`
2. اضغط **New repository secret**
3. أضف السرّين التاليين:

**Secret 1:**

- Name: `TAURI_PRIVATE_KEY`
- Value: محتوى ملف `updater-keys.key` كاملاً

**Secret 2:**

- Name: `TAURI_KEY_PASSWORD`
- Value: كلمة المرور (إذا تم إنشاؤها، وإلا اتركها فارغة)

### الخطوة 5: إضافة ملف .gitignore

أضف هذا السطر إلى `.gitignore`:

```
# Updater private keys
updater-keys.key
*.key
```

### الخطوة 6: رفع التغييرات

```powershell
git add .
git commit -m "Add auto-update system"
git push origin main
```

---

## 📦 نشر أول إصدار

### 1. تحديث أرقام الإصدارات

تأكد من أن جميع الملفات تحتوي على نفس رقم الإصدار (مثلاً `1.0.0`):

- ✅ `DAS Frontend/src-tauri/tauri.conf.json` → `"version": "1.0.0"`
- ✅ `DAS Frontend/src-tauri/Cargo.toml` → `version = "1.0.0"`
- ✅ `DAS Backend/backend/app/main.py` → `"version": "1.0.0"`
- ✅ `version.json` → `"version": "1.0.0"`

### 2. إنشاء Tag ورفعه

```powershell
git add .
git commit -m "Release v1.0.0"
git tag v1.0.0
git push origin main
git push origin v1.0.0
```

### 3. مراقبة البناء

1. اذهب إلى: `https://github.com/Qais9alnoS/Dasystem2/actions`
2. راقب workflow "Release Build and Deploy"
3. انتظر حتى يكتمل البناء (قد يستغرق 10-15 دقيقة)

### 4. التحقق من Release

1. اذهب إلى: `https://github.com/Qais9alnoS/Dasystem2/releases`
2. يجب أن تجد Release جديد بعنوان "Release v1.0.0"
3. تحقق من وجود الملفات:
   - `Educore_1.0.0_x64_en-US.msi`
   - `Educore_1.0.0_x64_en-US.msi.zip`
   - `Educore_1.0.0_x64_en-US.msi.zip.sig`
   - `latest.json`

---

## 🔄 نشر تحديث جديد (مثال: v1.0.1)

### 1. تحديث الكود

قم بإجراء التعديلات المطلوبة على الكود...

### 2. تحديث أرقام الإصدارات

غيّر رقم الإصدار في جميع الملفات إلى `1.0.1`:

```powershell
# تحديث يدوي للملفات:
# - DAS Frontend/src-tauri/tauri.conf.json
# - DAS Frontend/src-tauri/Cargo.toml
# - DAS Backend/backend/app/main.py
# - version.json
```

### 3. رفع التحديث

```powershell
git add .
git commit -m "Release v1.0.1 - Fix bugs and improvements"
git tag v1.0.1
git push origin main
git push origin v1.0.1
```

### 4. ماذا يحدث تلقائياً؟

1. **GitHub Actions** يبدأ تلقائياً عند رفع Tag
2. يتم بناء Backend و Frontend
3. يتم إنشاء Release جديد
4. **المستخدمون** الذين لديهم v1.0.0 سيرون نافذة تحديث عند فتح البرنامج
5. يمكنهم تحميل وتثبيت التحديث بضغطة زر

---

## 🧪 اختبار النظام

### اختبار محلي (قبل النشر):

```powershell
cd "DAS Frontend"
npm run tauri dev
```

### اختبار التحديث:

1. ثبّت الإصدار v1.0.0 على جهاز تجريبي
2. انشر v1.0.1 على GitHub
3. افتح البرنامج المثبت
4. يجب أن تظهر نافذة التحديث تلقائياً

---

## 📋 قائمة التحقق النهائية

قبل نشر أول إصدار، تأكد من:

- [ ] تم تثبيت الحزم: `npm install`
- [ ] تم توليد المفاتيح: `npm run tauri signer generate`
- [ ] تم تحديث `tauri.conf.json` بالمفتاح العام
- [ ] تم إضافة Secrets إلى GitHub
- [ ] تم إضافة `*.key` إلى `.gitignore`
- [ ] جميع أرقام الإصدارات متطابقة
- [ ] تم اختبار البناء محلياً

---

## ⚠️ ملاحظات مهمة

1. **المفتاح الخاص**: لا تشاركه أبداً ولا ترفعه على Git
2. **رقم الإصدار**: يجب أن يكون أكبر من السابق دائماً
3. **GitHub Secrets**: تأكد من إضافتها بشكل صحيح
4. **الاختبار**: اختبر على جهاز تجريبي قبل النشر للمستخدمين

---

## 🆘 حل المشاكل الشائعة

### المشكلة: GitHub Actions فشل

**الحل:**

1. تحقق من Secrets في GitHub
2. تحقق من صحة ملف `release.yml`
3. راجع logs في GitHub Actions

### المشكلة: التحديث لا يظهر

**الحل:**

1. تأكد من رقم الإصدار أكبر من الحالي
2. تحقق من المفتاح العام في `tauri.conf.json`
3. تحقق من وجود `latest.json` في Release

### المشكلة: خطأ في التوقيع

**الحل:**

1. تأكد من استخدام نفس المفتاح الخاص
2. تحقق من صحة المفتاح العام
3. أعد توليد المفاتيح إذا لزم الأمر

---

## 📞 الدعم

للمزيد من المعلومات، راجع:

- `DEPLOYMENT_GUIDE.md` - دليل النشر الشامل
- [Tauri Updater Docs](https://v2.tauri.app/plugin/updater/)
- [GitHub Actions Docs](https://docs.github.com/en/actions)
